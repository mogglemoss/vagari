/* ── Ministry warm dark palette (shared with HARUSPEX) ── */
$rust:        #C15F3C;
$rust-dim:    #8A3820;
$warm-bg:     #1a1815;
$warm-surface:#201d18;
$warm-panel:  #252118;
$warm-text:   #e8e6e3;
$warm-muted:  #7a756e;
$warm-border: #3a3530;

Screen {
    background: $warm-bg;
    color: $warm-text;
}

Footer {
    background: $warm-surface;
    color: $warm-muted;
}

#body {
    height: 1fr;
}

#chain-tree, #detail-panel {
    border: round $warm-border;
    border-title-color: $warm-muted;
    border-title-style: bold;
}
#chain-tree:focus, #detail-panel:focus {
    border: round $rust;
    border-title-color: $rust;
}

#chain-tree {
    width: 3fr;
    padding: 0 1;
    background: $warm-bg;
}

#detail-panel {
    width: 2fr;
    height: 100%;
    padding: 1 2;
    background: $warm-surface;
    border-left: solid $warm-border;
}

#status-line {
    height: 1;
    background: $warm-surface;
    color: $warm-muted;
    padding: 0 1;
}

#command-bar {
    height: 1;
    background: $warm-panel;
    border: none;
    color: $warm-text;
    padding: 0 1;
}
#command-bar:focus {
    border: none;
    background: $warm-panel;
}
#command-bar > .input--cursor {
    background: $rust;
    color: $warm-bg;
}
#command-bar > .input--placeholder {
    color: $warm-muted;
}

/* Command palette — Ministry issue, not factory default */
CommandPalette {
    background: $warm-surface 40%;
}
CommandPalette > Vertical {
    background: $warm-surface;
    border: round $warm-border;
}
CommandPalette > .command-palette--highlight {
    color: $rust;
    text-style: bold;
}
CommandPalette Input {
    background: $warm-panel;
    border: tall $warm-border;
    color: $warm-text;
}
CommandPalette Input:focus {
    border: tall $rust;
}
CommandPalette OptionList {
    background: $warm-surface;
    border: none;
}
CommandPalette OptionList > .option-list--option-highlighted {
    background: $rust 20%;
    color: $warm-text;
}
CommandPalette OptionList > .option-list--option-hover {
    background: $warm-panel;
}

/* Kill the default blue focus ring everywhere */
*:focus {
    border: round $warm-border;
    outline: none;
}

Tree {
    background: $warm-bg;
}
Tree:focus {
    border: none;
    background: $warm-bg;
}
Tree > .tree--cursor {
    background: $rust 25%;
    color: $warm-text;
}
Tree > .tree--highlight {
    background: $rust 12%;
}
Tree > .tree--highlight-line {
    background: $warm-panel;
}
Tree > .tree--guides {
    color: $warm-border;
}
Tree > .tree--guides-hover {
    color: $rust-dim;
}
Tree > .tree--guides-selected {
    color: $rust;
}

/* Warm scrollbars — Textual defaults to blue */
* {
    scrollbar-color: #5a5550;
    scrollbar-background: $warm-bg;
    scrollbar-color-hover: $rust;
    scrollbar-background-hover: $warm-bg;
    scrollbar-color-active: $rust;
    scrollbar-background-active: $warm-bg;
}

/* Watchtower pulse: the map's frame runs hot for a moment. */
#chain-tree.alerting {
    border: heavy #d4a017;
}

Toast {
    background: $warm-panel;
    border: round #d4a017;
}

/* Dossier widgets */
#dossier-body {
    height: auto;
}
#dossier-eol {
    height: 3;
    color: #d4a017;
    margin: 0 0 0 1;
}
#dossier-trend {
    height: 1;
    margin: 0 1;
}
#dossier-trend > .sparkline--max-color {
    color: #d4a017;
}
#dossier-trend > .sparkline--min-color {
    color: #3a3530;
}
/* DataTable — de-blue every stateful part (ported from HARUSPEX). */
DataTable {
    background: transparent;
    color: $warm-text;
}
DataTable > .datatable--header {
    background: $warm-surface;
    color: #a09890;
    text-style: bold;
}
DataTable > .datatable--header-hover {
    background: $warm-surface;
    color: $rust;
}
DataTable > .datatable--cursor {
    background: $rust 25%;
    color: $warm-text;
}
DataTable > .datatable--hover {
    background: $rust 12%;
    color: $warm-text;
}
DataTable > .datatable--even-row {
    background: #1e1b16;
}
DataTable > .datatable--odd-row {
    background: transparent;
}

#dossier-sigs {
    height: auto;
    max-height: 14;
    margin: 1 0 0 0;
}
